@model MenuLink
@{
	var activeClass = Model.IsActive ? "active" : "";
}

<a
	class="nav-link @activeClass"
	href="@Model.Url"
	title="@Model.Alt"
	aria-label="@Model.Alt">
	@Model.DisplayingTitle
</a>