@model SecondaryViewModel
<!DOCTYPE html>
<html lang="en">
    <head>
        @Html.Partial("Partials/Headers")
    </head>
    <body class="body">
        <!-- Header -->
        @Html.Partial("Partials/Header", Model)

        @RenderBody()
        
        <!-- Information -->
        @Html.Partial("Partials/Information", Model)

        <!-- Footer -->
        @Html.Partial("Partials/Footer", Model)

        <!-- Scripts -->
        @Html.Partial("Partials/Scripts", Model)

    </body>
</html>